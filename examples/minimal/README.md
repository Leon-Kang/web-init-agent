# Minimal example

This dependency-free TypeScript module proves that the starter can lint, type-check, build, and
test real source code before a framework is selected.

Run the full acceptance suite from the repository root:

```bash
pnpm run verify
```

Replace this directory after choosing a framework, but keep equivalent build and test coverage.
